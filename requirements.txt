# ---- Agent framework ----
crewai==0.86.0
crewai-tools==0.17.0
# crewai pins litellm>=1.44.22 with no upper bound. Left unpinned, pip resolves to
# a far newer litellm than crewai 0.86.0 was built against and backtracks through
# hundreds of releases. Pin the contemporaneous version.
litellm==1.53.9

# ---- LLM framework ----
langchain==0.3.14
langchain-community==0.3.14
langchain-openai==0.2.14
langchain-text-splitters==0.3.5

# ---- Backend ----
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
python-multipart==0.0.20
# EmailStr validation in request schemas.
email-validator==2.2.0

# ---- Auth ----
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
# passlib 1.7.4 reads bcrypt.__about__, which bcrypt 4.1 removed.
bcrypt==4.0.1

# ---- Database ----
sqlalchemy==2.0.36

# ---- Vector database ----
chromadb==0.5.23

# ---- MCP ----
mcp==1.2.0

# ---- Document ingestion (PDF / DOCX / TXT) ----
pypdf==5.1.0
docx2txt==0.8
# Chunk length is measured in tokens, not characters (see docs/RAG.md).
tiktoken==0.7.0

# ---- Config ----
python-dotenv==1.0.1

# ---- Transitive pins ----
# chromadb pulls httpx>=0.28 while litellm 1.53.9 requires <0.28. Unpinned,
# pip resolves to an httpx that litellm refuses to import at runtime.
httpx==0.27.2
